

<van-tabbar active="{{ active }}" bind:change="onChange" active-color="black">
  <van-tabbar-item wx:for="{{list}}"  wx:key="index">
  <image slot="icon" src="{{item.iconPath}}" style="width: 50rpx; height:50rpx;" />
  <image slot="icon-active" src="{{item.selectedIconPath}}" style="width: 50rpx; height:50rpx; "/>
  {{item.text}}
  </van-tabbar-item>
     
</van-tabbar>

